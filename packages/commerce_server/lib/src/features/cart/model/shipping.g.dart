// GENERATED CODE - DO NOT MODIFY BY HAND
// coverage:ignore-file
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, deprecated_member_use
// ignore_for_file: deprecated_member_use_from_same_package
// ignore_for_file: use_function_type_syntax_for_parameters
// ignore_for_file: unnecessary_const, avoid_init_to_null
// ignore_for_file: invalid_override_different_default_values_named
// ignore_for_file: prefer_expression_function_bodies
// ignore_for_file: annotate_overrides, invalid_annotation_target
// ignore_for_file: unnecessary_question_mark
// Generated by dust.

part of 'shipping.dart';

mixin _$ShippingOptionRow {
  @override
  String toString() {
    final self = this as ShippingOptionRow;
    return 'ShippingOptionRow('
        'amount: ${self.amount}, '
        'currencyCode: ${self.currencyCode}, '
        'id: ${self.id}, '
        'name: ${self.name}, '
        'regionId: ${self.regionId}'
        ')';
  }

  @override
  bool operator ==(Object other) {
    final self = this as ShippingOptionRow;
    return identical(this, other) ||
        other is ShippingOptionRow &&
            runtimeType == other.runtimeType &&
            other.amount == self.amount &&
            other.currencyCode == self.currencyCode &&
            other.id == self.id &&
            other.name == self.name &&
            other.regionId == self.regionId;
  }

  @override
  int get hashCode {
    final self = this as ShippingOptionRow;
    return Object.hashAll([
      runtimeType,
      self.amount,
      self.currencyCode,
      self.id,
      self.name,
      self.regionId,
    ]);
  }
}

mixin _$ShippingMethodRow {
  @override
  String toString() {
    final self = this as ShippingMethodRow;
    return 'ShippingMethodRow('
        'amount: ${self.amount}, '
        'name: ${self.name}, '
        'optionId: ${self.optionId}'
        ')';
  }

  @override
  bool operator ==(Object other) {
    final self = this as ShippingMethodRow;
    return identical(this, other) ||
        other is ShippingMethodRow &&
            runtimeType == other.runtimeType &&
            other.amount == self.amount &&
            other.name == self.name &&
            other.optionId == self.optionId;
  }

  @override
  int get hashCode {
    final self = this as ShippingMethodRow;
    return Object.hashAll([
      runtimeType,
      self.amount,
      self.name,
      self.optionId,
    ]);
  }
}

ShippingOptionRow _$ShippingOptionRowFromRow(Row row) {
  return ShippingOptionRow(
    id: row.read<String>('id'),
    regionId: row.read<String>('region_id'),
    name: row.read<String>('name'),
    amount: row.read<int>('amount'),
    currencyCode: row.read<String>('currency_code'),
  );
}

/// Row deserializer for [ShippingOptionRow].
final class $ShippingOptionRowRowDeserializer implements RowDeserializer<ShippingOptionRow> {
  const $ShippingOptionRowRowDeserializer();

  @override
  ShippingOptionRow deserialize(Row row) => _$ShippingOptionRowFromRow(row);
}

/// Typed row query terminals for [ShippingOptionRow].
///
/// Resolved from the static type of the receiver, so a row type with no
/// `FromRow` has no terminals and the call does not compile.
extension $ShippingOptionRowQuery on QueryAs<ShippingOptionRow> {
  /// Fetches exactly one row.
  Future<ShippingOptionRow> fetchOne(DatabaseExecutor db) =>
      fetchOneWith(db, _$ShippingOptionRowFromRow);

  /// Fetches zero or one row.
  Future<ShippingOptionRow?> fetchOptional(DatabaseExecutor db) =>
      fetchOptionalWith(db, _$ShippingOptionRowFromRow);

  /// Fetches every row.
  Future<List<ShippingOptionRow>> fetchAll(DatabaseExecutor db) =>
      fetchAllWith(db, _$ShippingOptionRowFromRow);
}

ShippingMethodRow _$ShippingMethodRowFromRow(Row row) {
  return ShippingMethodRow(
    optionId: row.read<String>('option_id'),
    name: row.read<String>('name'),
    amount: row.read<int>('amount'),
  );
}

/// Row deserializer for [ShippingMethodRow].
final class $ShippingMethodRowRowDeserializer implements RowDeserializer<ShippingMethodRow> {
  const $ShippingMethodRowRowDeserializer();

  @override
  ShippingMethodRow deserialize(Row row) => _$ShippingMethodRowFromRow(row);
}

/// Typed row query terminals for [ShippingMethodRow].
///
/// Resolved from the static type of the receiver, so a row type with no
/// `FromRow` has no terminals and the call does not compile.
extension $ShippingMethodRowQuery on QueryAs<ShippingMethodRow> {
  /// Fetches exactly one row.
  Future<ShippingMethodRow> fetchOne(DatabaseExecutor db) =>
      fetchOneWith(db, _$ShippingMethodRowFromRow);

  /// Fetches zero or one row.
  Future<ShippingMethodRow?> fetchOptional(DatabaseExecutor db) =>
      fetchOptionalWith(db, _$ShippingMethodRowFromRow);

  /// Fetches every row.
  Future<List<ShippingMethodRow>> fetchAll(DatabaseExecutor db) =>
      fetchAllWith(db, _$ShippingMethodRowFromRow);
}
