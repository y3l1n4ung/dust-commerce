// GENERATED CODE - DO NOT MODIFY BY HAND
// coverage:ignore-file
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, deprecated_member_use
// ignore_for_file: deprecated_member_use_from_same_package
// ignore_for_file: use_function_type_syntax_for_parameters
// ignore_for_file: unnecessary_const, avoid_init_to_null
// ignore_for_file: invalid_override_different_default_values_named
// ignore_for_file: prefer_expression_function_bodies
// ignore_for_file: annotate_overrides, invalid_annotation_target
// ignore_for_file: unnecessary_question_mark
// Generated by dust.

part of 'catalog_view_model.dart';

/// Base class generated for CatalogViewModel.
///
/// Extend it from your ViewModel and forward typed args:
///
/// ```dart
/// final class CatalogViewModel extends $CatalogViewModel {
///   CatalogViewModel(super.args);
/// }
/// ```
abstract class $CatalogViewModel extends ViewModelBase<CatalogState, CatalogViewModelArgs> {
  $CatalogViewModel(super.args) : super(initialState: const CatalogState());
}

/// Reads CatalogViewModel state from `BuildContext`.
///
/// Read `value` when the widget should rebuild for any state change.
///
/// ```dart
/// final state = context.watchCatalogViewModel().value;
/// ```
class _$CatalogViewModelProxy {
  _$CatalogViewModelProxy(this._context);

  final BuildContext _context;

  CatalogState get value {
    return CatalogViewModelScope.of(_context).value;
  }
}

/// Rebuilds when a selected CatalogState value changes.
///
/// Use this for widgets that depend on one field or derived value.
class CatalogViewModelSelector<R> extends StatefulWidget {
  const CatalogViewModelSelector({
    super.key,
    required this.selector,
    required this.builder,
    this.child,
    this.equals,
  });

  final R Function(CatalogState state) selector;
  final Widget Function(BuildContext context, R value, Widget? child) builder;
  final Widget? child;
  final bool Function(R previous, R next)? equals;

  @override
  State<CatalogViewModelSelector<R>> createState() => _CatalogViewModelSelectorState<R>();
}

class _CatalogViewModelSelectorState<R> extends State<CatalogViewModelSelector<R>> {
  CatalogViewModel? _viewModel;
  R? _selected;
  bool _hasSelected = false;

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
    final nextViewModel = CatalogViewModelScope._watchInstance(context);
    if (identical(_viewModel, nextViewModel)) return;
    _viewModel?.removeListener(_onViewModelStateChanged);
    _viewModel = nextViewModel;
    nextViewModel.addListener(_onViewModelStateChanged);
    _selectCurrent(force: true);
  }

  @override
  void didUpdateWidget(CatalogViewModelSelector<R> oldWidget) {
    super.didUpdateWidget(oldWidget);
    _selectCurrent(force: true);
  }

  void _onViewModelStateChanged() {
    _selectCurrent();
  }

  void _selectCurrent({bool force = false}) {
    final viewModel = _viewModel;
    if (viewModel == null) return;
    final nextSelected = widget.selector(viewModel.value);
    if (!_hasSelected) {
      _selected = nextSelected;
      _hasSelected = true;
      return;
    }
    final previousSelected = _selected as R;
    final equals = widget.equals;
    final unchanged = equals == null
        ? previousSelected == nextSelected
        : equals(previousSelected, nextSelected);
    if (!force && unchanged) return;
    if (force || !mounted) {
      _selected = nextSelected;
    } else {
      setState(() {
        _selected = nextSelected;
      });
    }
  }

  @override
  void dispose() {
    _viewModel?.removeListener(_onViewModelStateChanged);
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    if (!_hasSelected) {
      _selectCurrent(force: true);
    }
    return widget.builder(context, _selected as R, widget.child);
  }
}

/// Creates and provides CatalogViewModel to descendants.
///
/// The default constructor owns the ViewModel. Use `.value` for externally
/// owned ViewModels.
///
/// ```dart
/// CatalogViewModelScope(
///   args: (context) => CatalogViewModelArgs(...),
///   create: (context, args) => CatalogViewModel(args),
///   child: const FeaturePage(),
/// )
/// ```
class CatalogViewModelScope extends StatefulWidget {
  /// Creates and owns CatalogViewModel from typed args.
  const CatalogViewModelScope({
    super.key,
    required this.args,
    required this.create,
    required this.child,
    this.identity,
  }) : value = null;

  /// Provides an externally owned CatalogViewModel without disposing it.
  const CatalogViewModelScope.value({
    super.key,
    required CatalogViewModel this.value,
    required this.child,
  }) : args = null,
       create = null,
       identity = null;

  final CatalogViewModelArgs Function(BuildContext context)? args;
  final CatalogViewModel Function(BuildContext context, CatalogViewModelArgs args)? create;
  final Object? Function(BuildContext context)? identity;
  final CatalogViewModel? value;
  final Widget child;

  /// Reads CatalogViewModel without rebuilding when state changes.
  static CatalogViewModel read(BuildContext context) {
    final scope = context
        .getElementForInheritedWidgetOfExactType<_CatalogViewModelInstance>()
        ?.widget as _CatalogViewModelInstance?;
    if (scope == null) throw StateError('No CatalogViewModelScope found in context.');
    return scope.viewModel;
  }

  static CatalogViewModel _watchInstance(BuildContext context) {
    final scope = context.dependOnInheritedWidgetOfExactType<_CatalogViewModelInstance>();
    if (scope == null) throw StateError('No CatalogViewModelScope found in context.');
    return scope.viewModel;
  }

  /// Watches CatalogViewModel and rebuilds when state changes.
  static CatalogViewModel of(BuildContext context) {
    final scope = context.dependOnInheritedWidgetOfExactType<_CatalogViewModelInherited>();
    if (scope == null) throw StateError('No CatalogViewModelScope found in context.');
    return scope.viewModel;
  }

  @override
  State<CatalogViewModelScope> createState() => _CatalogViewModelScopeState();
}

class _CatalogViewModelScopeState extends State<CatalogViewModelScope> {
  CatalogViewModel? _viewModel;
  Object? _identity;
  bool _ownsViewModel = false;

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
    final external = widget.value;
    if (external != null) {
      _replaceViewModel(external, ownsViewModel: false, notify: false);
      return;
    }
    final nextIdentity = widget.identity?.call(context);
    if (_viewModel == null || nextIdentity != _identity) {
      _identity = nextIdentity;
      _replaceViewModel(_createOwnedViewModel(), ownsViewModel: true, notify: false);
    }
  }

  @override
  void didUpdateWidget(CatalogViewModelScope oldWidget) {
    super.didUpdateWidget(oldWidget);
    final external = widget.value;
    if (external != null) {
      _replaceViewModel(external, ownsViewModel: false);
    } else if (oldWidget.value != null) {
      _identity = widget.identity?.call(context);
      _replaceViewModel(_createOwnedViewModel(), ownsViewModel: true);
    } else {
      final nextIdentity = widget.identity?.call(context);
      if (nextIdentity != _identity) {
        _identity = nextIdentity;
        _replaceViewModel(_createOwnedViewModel(), ownsViewModel: true);
      }
    }
  }

  CatalogViewModel _createOwnedViewModel() {
    final argsFactory = widget.args;
    final create = widget.create;
    if (argsFactory == null || create == null) {
      throw StateError('Owned CatalogViewModelScope requires args and create.');
    }
    late final CatalogViewModel created;
    try {
      created = create(context, argsFactory(context));
    } catch (error, stackTrace) {
      Error.throwWithStackTrace(error, stackTrace);
    }
    return created;
  }

  void _replaceViewModel(
    CatalogViewModel nextViewModel, {
    required bool ownsViewModel,
    bool notify = true,
  }) {
    final previous = _viewModel;
    if (identical(previous, nextViewModel)) {
      _ownsViewModel = ownsViewModel;
      _scheduleInit(nextViewModel);
      if (notify && mounted) setState(() {});
      return;
    }
    previous?.removeListener(_onViewModelStateChanged);
    if (_ownsViewModel) previous?.dispose();
    _viewModel = nextViewModel;
    _ownsViewModel = ownsViewModel;
    nextViewModel.addListener(_onViewModelStateChanged);
    _scheduleInit(nextViewModel);
    if (notify && mounted) setState(() {});
  }

  void _scheduleInit(CatalogViewModel viewModel) {
    scheduleMicrotask(() async {
      if (!mounted || !identical(_viewModel, viewModel)) return;
      try {
        await viewModel.init();
      } catch (error, stackTrace) {
        FlutterError.reportError(
          FlutterErrorDetails(
            exception: error,
            stack: stackTrace,
            library: 'dust state',
            context: ErrorDescription('CatalogViewModelScope init failed'),
          ),
        );
      }
    });
  }

  void _onViewModelStateChanged() {
    if (mounted) setState(() {});
  }

  @override
  void dispose() {
    final viewModel = _viewModel;
    viewModel?.removeListener(_onViewModelStateChanged);
    if (_ownsViewModel) viewModel?.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final viewModel = _viewModel;
    if (viewModel == null) {
      throw StateError('CatalogViewModelScope built before its view model was initialized.');
    }
    return _CatalogViewModelInstance(
      viewModel: viewModel,
      child: _CatalogViewModelInherited(
        viewModel: viewModel,
        state: viewModel.value,
        child: widget.child,
      ),
    );
  }
}

class _CatalogViewModelInstance extends InheritedWidget {
  const _CatalogViewModelInstance({required this.viewModel, required super.child});

  final CatalogViewModel viewModel;

  @override
  bool updateShouldNotify(_CatalogViewModelInstance oldWidget) {
    return !identical(viewModel, oldWidget.viewModel);
  }
}

class _CatalogViewModelInherited extends InheritedWidget {
  const _CatalogViewModelInherited({required this.viewModel, required this.state, required super.child});

  final CatalogViewModel viewModel;
  final CatalogState state;

  @override
  bool updateShouldNotify(_CatalogViewModelInherited oldWidget) {
    return !identical(viewModel, oldWidget.viewModel) || state != oldWidget.state;
  }
}

/// Handles one-shot effects from CatalogViewModel.
///
/// Effects are delivered without changing state and do not rebuild `child`.
///
/// ```dart
/// CatalogViewModelListener(
///   listener: onEffect,
///   child: const FeaturePage(),
/// )
/// ```
class CatalogViewModelListener extends StatefulWidget {
  const CatalogViewModelListener({super.key, required this.listener, required this.child});

  final void Function(BuildContext context, Object effect) listener;
  final Widget child;

  @override
  State<CatalogViewModelListener> createState() => _CatalogViewModelListenerState();
}

class _CatalogViewModelListenerState extends State<CatalogViewModelListener> {
  StreamSubscription<Object>? _sub;
  CatalogViewModel? _viewModel;

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
    final nextViewModel = CatalogViewModelScope._watchInstance(context);
    if (_viewModel == nextViewModel) return;
    _sub?.cancel();
    _viewModel = nextViewModel;
    _sub = nextViewModel.effects.listen(_onEffect);
  }

  void _onEffect(Object effect) {
    if (mounted) widget.listener(context, effect);
  }

  @override
  void dispose() {
    _sub?.cancel();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) => widget.child;
}

/// BuildContext helpers generated for CatalogViewModel.
///
/// ```dart
/// final vm = context.readCatalogViewModel();
/// final state = context.watchCatalogViewModel().value;
/// ```
extension CatalogViewModelBuildContext on BuildContext {
  _$CatalogViewModelProxy watchCatalogViewModel() {
    return _$CatalogViewModelProxy(this);
  }

  CatalogViewModel readCatalogViewModel() => CatalogViewModelScope.read(this);
}
